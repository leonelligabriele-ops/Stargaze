# Lite/free-tier deps — NO sentence-transformers / torch (set STARGAZE_LITE=1).
# Tiny install → fast, low-memory build that fits Render's free 512 MB instance.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
numpy>=2.1.0
rapidfuzz>=3.10.0
